<h1>All The Bears!</h1>

<ul>
  <%= for bear <- bears do %>
    <li><%= bear.name %> - <%= bear.type %></li>
  <% end %>
</ul>

<% # So in a template file, you always want to use inspect. %>
<%= # inspect(bears) %>